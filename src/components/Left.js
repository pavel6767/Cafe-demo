import React from "react";

export default function() {
  return (
    <div>
      <h1>Left</h1>
      <div>Table {"{...}"}</div>
      <button>Check in/out {"{...}"}</button>
      <div>Bill total {"{...}"}</div>
      <div>Orders {"{...}"}</div>
    </div>
  );
}
